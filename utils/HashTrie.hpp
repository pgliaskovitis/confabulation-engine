#include <string>
#include <vector>

template <typename T>
class HashTrie {
public:
    void Add(const std::vector<T>& sequence) {
//        if (sequence == null) {
//            return;
//        }
//        Node cur = root;
//        for (E e : sequence) {
//            cur = cur.put(e, false);
//        }
//        if (!cur.is_final()) {
//            cur.set_final(true);
//            size++;
//        }
    }

};
